[Unit]
Description=Restore MagicOS Calamares Customizations
After=local-fs.target
Before=calamares.service

[Service]
Type=oneshot
ExecStart=/usr/bin/bash -c "mkdir -p /etc/calamares && cp -rf /etc/calamares-custom/* /etc/calamares/"
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
